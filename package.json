{
  "name": "k8n",
  "version": "0.0.0",
  "private": true,
  "packageManager": "npm@11.11.1",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
